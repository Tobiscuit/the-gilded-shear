# Credentials Directory

This folder is for sensitive files that should NEVER be committed to git.

## What Goes Here:
- Firebase service account keys (.json)
- API keys stored in files
- SSL certificates for development
- Any other sensitive credentials

## What's Ignored:
Everything in this folder is ignored by .gitignore EXCEPT:
- This .gitkeep file
- README.md (instructions only)

## Security:
⚠️ NEVER commit files from this folder!
⚠️ NEVER share these files via Slack, email, etc.
✅ Use secure methods: 1Password, LastPass, or direct encrypted transfer

